# ScanoPort
Fast, dependency CLI TCP port scanner built in Python. Designed as an extensible framework for security researchers to perform targeted network scans and build custom penetration testing modules.
